import UIKit

extension UIImage {
    enum CheckBox {
        static let selected: UIImage? = UIImage(named: "selected")
        static let unselected: UIImage? = UIImage(named: "unselected")
    }
}
